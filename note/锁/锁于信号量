## 信号量

- 信号量：有多个资源，同时可以允许多个线程访问，进行的操作称为pv操作，p减少，v增加，互斥量为特殊的信号量，只有0和1两个值，同时只允许一个线程访问资源
- 生产者与消费者

## 锁

- 自旋锁
  自旋锁做多只能被一个线程访问资源，若锁被别人拿走之后，下一个来拿所的线程将会一直自旋等待锁的释放

- 读写锁
  可读写的资源分为读/写，即消费者与生产者，读对内存数据无影响，写对数据有影响，读不用加锁，写需要加锁